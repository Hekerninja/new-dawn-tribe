"use client"; import * as React from "react"; import { Moon, Sun } from "lucide-react"; import { useTheme } from "next-themes"; import { Switch } from "@/components/ui/switch"; export function ThemeToggle() { const { setTheme, theme } = useTheme(); const [mounted, setMounted] = React.useState(false); const [isDark, setIsDark] = React.useState(false); React.useEffect(() => { setMounted(true); setIsDark(theme === "dark"); }, [theme]); if (!mounted) { return ( <div className="w-10 h-6 bg-slate-200 dark:bg-slate-700 rounded-full relative transition-colors duration-300" /> ); } const handleToggle = () => { const newTheme = theme === "dark" ? "light" : "dark"; setTheme(newTheme); setIsDark(newTheme === "dark"); }; return ( <div className="flex items-center gap-2"> <Sun className="w-4 h-4 text-slate-500 dark:text-slate-400 transition-colors duration-300" /> <Switch checked={isDark} onCheckedChange={handleToggle} className="data-[state=checked]:bg-teal-600 data-[state=unchecked]:bg-slate-300 dark:data-[state=unchecked]:bg-slate-600 transition-colors duration-300" /> <Moon className="w-4 h-4 text-slate-500 dark:text-slate-400 transition-colors duration-300" /> </div> ); }