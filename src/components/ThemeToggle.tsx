"use client"; import * as React from "react"; import { Moon, Sun } from "lucide-react"; import { useTheme } from "next-themes"; import { Button } from "@/components/ui/button"; export function ThemeToggle() { const { setTheme, theme } = useTheme(); const [mounted, setMounted] = React.useState(false); React.useEffect(() => { setMounted(true); }, []); if (!mounted) { return ( <Button variant="ghost" size="icon" className="w-9 px-0"> <div className="w-5 h-5" /> </Button> ); } return ( <Button variant="ghost" size="icon" onClick={() => setTheme(theme === "dark" ? "light" : "dark")} className="w-9 px-0 text-slate-600 hover:text-teal-600 dark:text-slate-300 dark:hover:text-teal-400 transition-all duration-300 ease-in-out hover:bg-slate-100 dark:hover:bg-slate-800 rounded-full" > <div className="relative w-5 h-5"> {theme === "dark" ? ( <Sun className="absolute w-5 h-5 transition-all duration-300 ease-in-out transform rotate-90 opacity-0 scale-50" /> ) : ( <Moon className="absolute w-5 h-5 transition-all duration-300 ease-in-out transform -rotate-90 opacity-0 scale-50" /> )} {theme === "dark" ? ( <Moon className="w-5 h-5 transition-all duration-300 ease-in-out transform rotate-0 opacity-100 scale-100" /> ) : ( <Sun className="w-5 h-5 transition-all duration-300 ease-in-out transform rotate-0 opacity-100 scale-100" /> )} </div> <span className="sr-only">Toggle theme</span> </Button> ); }